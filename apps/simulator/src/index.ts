/**
 * Yük ve saha senaryosu iskeleti (SPEC §13, Faz 9).
 * Senaryo motoru henüz bağlanmadı; paket turbo/dev hattında yer tutar.
 */
function main(): void {
  process.stdout.write(
    'simulator: iskelet hazır. Senaryolar Faz 9 ile bağlanır (--vehicles, --scenario).\n',
  );
}

main();
